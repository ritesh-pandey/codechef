#include <iostream>
#include <cstdio>
#include <vector>
#include <math>

using namespace std;

int main(){
	int N;
	scanf("%d", &N);
	vector<int> b(N);
	for(int i=0; i<N; i++){
		scanf("%d",&b[i]);
	}
	int piece = sqrt(N);
	if(N % sqrt(N) == 0)
		vector<int> M(sqrt(N));
	else
		vector<int> M(sqrt(N));
	int piece = sqrt(N);
	for(int i=0; i<N; i++){
		
	}
	
}
